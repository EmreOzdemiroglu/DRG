# DRG Core Dependencies
# These are the minimum required dependencies for basic DRG functionality
# For feature-specific dependencies, use extras in pyproject.toml:
#   pip install drg[openai]        # For OpenAI embeddings
#   pip install drg[neo4j]         # For Neo4j graph persistence
#   pip install drg[api]           # For API server
#   pip install drg[all]           # For all optional dependencies

dspy>=2.5.0,<3.0.0
pydantic>=2.0.0

# Note: litellm is NOT required here - DSPy handles LLM providers internally
# Optional dependencies are defined in pyproject.toml [project.optional-dependencies]
# Use: pip install -e ".[feature]" or pip install drg[feature] for optional features
